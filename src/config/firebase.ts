// Firebase config - Auth is now handled via MSG91 OTP + backend JWT tokens
// Firebase is only used for FCM push notifications (via @react-native-firebase/messaging)
// The @react-native-firebase/auth package can be removed from package.json
