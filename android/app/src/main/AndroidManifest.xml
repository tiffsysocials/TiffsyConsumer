<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for FCM notifications on Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Remove AD_ID permissions injected by Firebase (not needed for this app) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Default notification icon: must be a white-on-transparent silhouette
           (Android 5+ requirement). The colored launcher icon used to be set here
           and rendered as a blank white square in the status bar. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_notification" />

      <!-- Optional: Set default notification color -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_color"
        android:resource="@color/primary"
        tools:replace="android:resource" />

      <!-- Optional: Set default notification channel for Android O+ -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="default_channel"
        tools:replace="android:value" />

      <!-- Google Maps API key — used by react-native-maps for tile rendering on Android.
           Restricted to Maps SDK for Android (separate from the Geocoding/Places HTTP key in
           src/constants/config.ts which is used by location.service.ts and LocationPickerScreen). -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyB4jJpH1-3SYsBGguA9UqHcwGs-AC_bpuw" />

      <!-- Remove adservices library injected by Firebase Analytics -->
      <uses-library android:name="android.ext.adservices" tools:node="remove" />
    </application>
</manifest>
